spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sneakerrun?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root          # ?? MySQL ??? (?? root)
    password: 1234          # ?? MySQL ???? (? ?????!)

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update      # ??: DB ???? ???? ????? ??
    properties:
      hibernate:
        format_sql: true    # ??? SQL? ??? ???
        show_sql: true      # ???? SQL? ??? ??